import React from 'react'

function DestructoringArray(props) {
    const {framework, name} = props
  return (
    <div>DestructoringArray using {framework} by {name}</div>
  )
}

export default DestructoringArray